import { navigation } from '../../navigation/navigation.data';
import { profile } from '../profile.data';
import { ProfileNavigation } from './profile-navigation.types';

export const profileNavigations: ProfileNavigation[] = [];
export const profileNavigation: ProfileNavigation = {
  id_profile_navigation: ' ',
  profile: profile,
  navigation: navigation,
};
